language: objective-c
script:
  - xctool -workspace UnrarKit.xcworkspace -scheme UnrarKit -configuration Release build test
  - xctool -workspace UnrarKit.xcworkspace -scheme UnrarExample -configuration Release -sdk iphonesimulator build
  - ./Scripts/cocoapod-validate.sh